const SITE_URL='https://kenteglobal.example';
export function upsertMeta(name,content){let node=document.querySelector(`meta[name="${name}"]`);if(!node){node=document.createElement('meta');node.setAttribute('name',name);document.head.appendChild(node);}node.setAttribute('content',content);}
export function upsertProperty(property,content){let node=document.querySelector(`meta[property="${property}"]`);if(!node){node=document.createElement('meta');node.setAttribute('property',property);document.head.appendChild(node);}node.setAttribute('content',content);}
export function updateSEO({title='KenteGlobal | Woven with meaning',description='Ghanaian-made Kente, African fashion, cultural objects, and meaningful commerce.',path='/',product=null}){document.title=title;upsertMeta('description',description);upsertProperty('og:title',title);upsertProperty('og:description',description);upsertProperty('og:type',product?'product':'website');upsertProperty('og:url',`${SITE_URL}${path}`);let canonical=document.querySelector('link[rel="canonical"]');if(!canonical){canonical=document.createElement('link');canonical.rel='canonical';document.head.appendChild(canonical);}canonical.href=`${SITE_URL}${path}`;let old=document.getElementById('kg-product-schema');if(old)old.remove();if(product){const script=document.createElement('script');script.id='kg-product-schema';script.type='application/ld+json';script.textContent=JSON.stringify({'@context':'https://schema.org','@type':'Product',name:product.name,description:product.description,image:[product.image],brand:{'@type':'Brand',name:'KenteGlobal'},offers:{'@type':'Offer',price:product.price,priceCurrency:product.currency||'USD',availability:product.stock>0?'https://schema.org/InStock':'https://schema.org/OutOfStock',url:`${SITE_URL}${path}`}});document.head.appendChild(script);}}
export { SITE_URL };
